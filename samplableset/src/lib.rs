mod binary_tree;
mod hash_propensity;
pub mod samplable_set;

pub use samplable_set::SamplableSet;

pub fn add(left: u64, right: u64) -> u64 {
    left + right
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn it_works() {
        let result = add(2, 2);
        assert_eq!(result, 4);
    }
}
